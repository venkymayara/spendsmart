import { HighlightOverBudget } from './highlight-over-budget';

describe('HighlightOverBudget', () => {
  it('should create an instance', () => {
    const directive = new HighlightOverBudget();
    expect(directive).toBeTruthy();
  });
});
